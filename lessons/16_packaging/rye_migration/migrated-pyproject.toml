[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "legacy-report-tool"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pydantic>=2,<3"]

[project.scripts]
report = "legacy_report.__main__:main"

[dependency-groups]
dev = ["pytest>=8"]
